# Reproducibility notes

- Use fixed config files under `configs/`.
- Keep dataset version naming stable.
- For optimization quality, record:
  - `optimization.max_offset`
  - `optimization.iterations`
  - `optimization.learning_rate`
