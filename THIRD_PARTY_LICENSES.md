# Third-party licenses

No third-party source code is currently bundled in this repository.

Runtime dependencies are installed from package indexes (see `requirements.txt`).
If you vendor external source repositories in `third_party/`, list them here with:
- project name
- source URL
- commit hash
- license file path
